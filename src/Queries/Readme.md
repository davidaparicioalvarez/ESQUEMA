# Queries
English: Predefined data retrieval (joins, filters) for performance/API.
Spanish: Recuperación de datos predefinida (joins, filtros) para rendimiento/API.
Guidelines:
- Use for complex joins.
- Prefer SetRange/SetFilter in code for simple needs.