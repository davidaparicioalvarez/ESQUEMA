# dotnet
English: Declares allowed .NET types for interoperability (cloud-safe subset).
Spanish: Declara tipos .NET permitidos para interoperabilidad (subset seguro en la nube).
Guidelines:
- Use only when AL lacks feature.
- Keep surface minimal for upgrade safety.
Reference: [src/dotnet/dotnet.al](src/dotnet/dotnet.al)